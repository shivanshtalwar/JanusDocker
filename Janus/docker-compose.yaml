version: "3.9" # optional since v1.27.0
services:
  janus-multistreaming:
    network_mode: "host"
    build:
      context: .
      args:
        JANUS_COMMIT_HASH: ${JANUS_COMMIT_HASH}
        URI_CONV_ENDPOINT: ${JANUS_URI_CONV_ENDPOINT}
        URI_CONV_AUTH_TOKEN: ${JANUS_URI_CONV_AUTH_TOKEN}
